class MainModel {
  String title;
  String description;
  String symbol;
  String url;
  List<String> directions = [];
  MainModel(
      {this.title, this.description, this.symbol, this.url, this.directions});
}
