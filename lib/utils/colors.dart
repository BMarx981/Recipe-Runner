import 'package:flutter/material.dart';

const Color mainTheme = Color(0xff992233);
const List<Color> colors = [Color(0xff2211ff), Color(0xffBB4455)];
const List<double> stops = [0.0, 0.3];
const LinearGradient colorGrad = LinearGradient(colors: colors, stops: stops);
const Color white = Colors.white;
