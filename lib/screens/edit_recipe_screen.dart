import 'package:flutter/material.dart';

class EditRecipeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('Edit this shit!!'),
        ),
      ),
    );
  }
}
